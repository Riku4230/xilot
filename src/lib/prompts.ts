export interface QuickAction {
  id: string;
  icon: string;
  label: string;
  buildPrompt: (articleText: string) => string;
}

export const QUICK_ACTIONS: QuickAction[] = [
  {
    id: "infographic",
    icon: "📊",
    label: "概要画像",
    buildPrompt: (articleText) => {
      const summary = articleText.slice(0, 2000);
      return `image_genツールを使って今すぐ画像を1枚生成してください。説明は不要です。直接ツールを呼び出してください。

以下の記事の要点を1枚の横長インフォグラフィック（1920×1080px、16:9）にまとめた画像を生成してください。

デザイン: 白背景、青(#1d9bf0)アクセント、モダンsans-serif。左側にタイトルと要約、右側にキーポイント3〜5個をアイコン付きで配置。テキストは日本語で、完全に読めるサイズにすること。余計な装飾やウォーターマークなし。

記事の内容（要約）:
${summary}`;
    },
  },
  {
    id: "summary",
    icon: "📝",
    label: "要約",
    buildPrompt: (articleText) =>
      `以下の記事を日本語で要約してください。

## 出力形式
1. **一言まとめ**（1文、記事の本質を捉える）
2. **要約**（3〜5文、記事の流れと主張を簡潔に）
3. **対象読者**（この記事が最も役立つ人）

## ルール
- 原文にない主張を追加しない
- 専門用語は必要に応じて簡単な説明を括弧で補足
- 簡潔さを優先し、冗長な表現を避ける

## 記事の内容
${articleText}`,
  },
  {
    id: "keypoints",
    icon: "🔑",
    label: "キーポイント",
    buildPrompt: (articleText) =>
      `以下の記事から重要なポイントを抽出し、日本語の箇条書きにしてください。

## 出力形式
- 各ポイントは1〜2文で簡潔に
- 5〜10個のポイントを重要度順に
- 各ポイントの先頭に適切な絵文字を1つ付ける
- 記事に含まれる具体的な数字・ツール名・手法は省略せず含める

## ルール
- 原文に書かれていることだけを抽出する
- 抽象的な言い換えより、具体的な情報を優先する

## 記事の内容
${articleText}`,
  },
  {
    id: "actionable",
    icon: "💡",
    label: "実践Tips",
    buildPrompt: (articleText) =>
      `以下の記事の内容を、読者がすぐに実践できるアクションリストに変換してください。

## 出力形式
**今日からできること：**
1. [具体的なアクション]（所要時間の目安）
2. ...

**1週間以内にやること：**
1. ...

**長期的に取り組むこと：**
1. ...

## ルール
- 記事に基づいた具体的なアクションのみ
- 各アクションは「〜する」の形で、曖昧な表現を避ける
- 記事で言及されたツールや手法があれば具体名を含める
- アクションごとに想定される所要時間を括弧で添える

## 記事の内容
${articleText}`,
  },
];
